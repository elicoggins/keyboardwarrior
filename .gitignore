# This repo holds no source and no build output — only the README, the notices,
# and whatever is attached to a GitHub Release. Staged release artifacts are
# built from the private source repo and uploaded, never committed: they're
# tens of megabytes each and Releases already stores them.
*.zip
*.tar.gz
*.dmg
*.app/
dist/

# OS junk
.DS_Store
**/.DS_Store
